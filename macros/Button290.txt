$${

TOGGLE(@alerta);
    IF(@alerta);
        SETLABEL("StatusALERTA","\n\n&6Alerta de Armadura: &2ON &f(&c%@#durabilidade%&f)");
        SET(@&info_msg,"&aAlerta de Armadura no Vermelho ativado.");
    ELSE;
        SETLABEL("StatusALERTA","\n\n&6Alerta de Armadura: &4OFF");
        SET(@&info_msg,"&cAlerta de Armadura no Vermelho desativado.");
    ENDIF;
EXEC(Alerta.txt,ALERTA); 
SET(@#info_time,"60");
EXEC(Info.txt,INFO);

}$$